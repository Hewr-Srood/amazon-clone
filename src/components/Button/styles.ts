import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  btn: {
    backgroundColor: '#ffae3d',
    marginTop: 10,
    marginBottom: 15,
    height: 45,

    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 5,
    // padding: 10,
  },
  btnText: {
    fontSize: 16,
    color: '#525252',
  },
});
export default styles;
